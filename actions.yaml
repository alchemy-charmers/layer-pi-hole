example-action:
  description: "This is just a test"
set-password:
  description: "Set the password for the web interface"
  params:
    password:
      type: string
      description: "The password to set"
